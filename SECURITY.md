# Security

## Reporting a Vulnerability

Email **bensxnisaac@gmail.com** with details. Do not file a public issue.

## Best Practices

- Never commit API keys or secrets
- Use environment variables for provider credentials
- Docker builds should use build secrets, not env vars
